version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - errorlint
    - gocritic
    - gosec
    - misspell
    - nilerr
    - unconvert
    - unparam
    - whitespace

  settings:
    gosec:
      excludes:
        - G104
    gocritic:
      disabled-checks:
        - ifElseChain

  exclusions:
    generated: lax
    rules:
      - path: examples/
        linters:
          - gosec
      - path: _test\.go$
        linters:
          - gosec
          - unparam

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/slam0504/go-ddd-core

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
